import React from "react";

const ParallaxSection: React.FC = () => {
  return (
    <section className="parallax h-screen bg-[url('/images/home/parallax-background.jpg')] flex items-center justify-center text-white text-center"></section>
  );
};

export default ParallaxSection;
